[project]
name = "object-harvest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
obh-detect = "obh.detect:main"
obh-generate = "obh.generate:main"
